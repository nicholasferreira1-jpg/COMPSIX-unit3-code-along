// dot Notation
const mathUtils= require("./mathUtils")
console.log(mathUltils.add(5,9));

// Destuctive ->
const {add, subtract, divide, multiply, calculateAverage} = require('./mathUtils.js');
// The destructing way is considered the standard practice
console.log(add(5,3))

